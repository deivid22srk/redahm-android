<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ReDAHMTheme" parent="android:Theme.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Launcher screen: normal (non-fullscreen) dark theme. -->
    <style name="ReDAHMLauncherTheme" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>